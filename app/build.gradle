apply plugin: 'com.android.application'

android {
    compileSdkVersion 35
//    buildToolsVersion "26.0.2"

    defaultConfig {
        applicationId "com.github.epd.sprout.test"
        minSdkVersion 14
        targetSdkVersion 35
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
